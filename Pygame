import pygame
pygame.init()
import time
import random

# Initialize pygame
pygame.init()

# Set up display
width, height = 1920, 1080
win = pygame.display.set_mode((width, height))
pygame.display.set_caption("Snake Game")
